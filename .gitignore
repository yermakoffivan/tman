bin/
obj/

# tman run logs — last run's output + failure digest, rewritten every run
/.tman/

# tman shims
/test
/test.cmd

# worktrack loop state — per-machine lease + session notes, not shared history
.worktrack/*
!.worktrack/mcp.json
!.worktrack/templates.json
